# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GithubActionsWithExtraSteps (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_build --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: build

on: [push, pull_request]

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: 'Cache: .nuke/temp, ~/.nuget/packages, **/node_modules'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
            **/node_modules
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/package.json', '**/yarn.lock') }}
      - name: 'Setup: actions/setup-node@v4'
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: 'Run: EnableCorepack, YarnInstall, YarnBuild, YarnTest'
        run: ./build.cmd EnableCorepack YarnInstall YarnBuild YarnTest
